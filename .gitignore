/sbi-logs/
/archive/
/.claude/

# A 6.9 MB run log that was once committed to the repo root; untracked and deleted by the clean
# break (2026-09-11). Ignoring it stops it coming back.
/sbc_run.log
*.pyc
__pycache__/

# Generated artifacts (piece 1 of the 2026-09-10 hardening programme): every generated file lives
# under Artifacts/<kind>/<name>__<id>/ with a manifest.json. The old Resources/{Plots,Priors,
# Posteriors,CrossValidation,ReductionMap,Checkpoints,Observations} trees were deleted by the
# clean-break runbook on 2026-09-11; Resources/ now holds only the hand-edited inputs.
/Artifacts/

# pytest
/.pytest_cache/
