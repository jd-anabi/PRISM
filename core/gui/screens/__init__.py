"""Screens for the MAPPI navigation shell: a home/splash screen and one screen per section, switched
by a NavShell. Import the concrete screens from their submodules (nav_shell, home_screen,
section_screen, inference_screen) -- this package init is intentionally empty, matching core/gui."""
