# NOTE: this file targets the PLANNED upgrade (sbi 0.26.1 + torch 2.9.0), which is a matched pair --
# sbi 0.25.0 requires torch<2.6.0, so the two move together. It does NOT describe the currently
# installed/tested env, which is sbi 0.25.0 + torch 2.5.1. Installing from this file WILL upgrade both.
# After upgrading, re-verify: (1) sbi still prints its per-epoch counter (the GUI cancel checkpoint --
# grep sbi for _maybe_show_progress / "Epochs trained"); (2) the calibration numbers (0.26's TARP
# changes make them non-comparable to the pre-upgrade handoff). See gui_handoff.txt (ENV NOTE).

--extra-index-url https://download.pytorch.org/whl/cu130

numpy==2.3.3
scipy==1.16.2
pandas==2.3.3
pyarrow==20.0.0
h5py==3.15.1
matplotlib==3.10.7
tqdm==4.67.1
sympy==1.14.0
torch==2.9.0+cu130
torchvision==0.24.0+cu130
pint==0.25
sbi==0.26.1
antropy==0.1.9
pybispectra==1.2.4
scikit-learn==1.7.2
corner==2.2.3
hdbscan==0.8.41
PySide6==6.9.3
