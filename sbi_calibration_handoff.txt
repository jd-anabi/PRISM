================================================================================
GFDT / SBI HANDOFF  —  Nadrowski hair-cell parameter inference
Updated 2026-07-13. Self-contained context for a new chat to continue the SBI work.
================================================================================

OPEN ISSUES / TODO (fix in a new chat)
  [x] DONE 2026-07-13 -- CELL FILES now hold VALUES + INITIAL CONDITIONS ONLY (no bounds, no units).
      RESOLUTION (all local): stripped all 8 cells to `name = val`; the "no code change" note was WRONG
      -- cli._parse_cell -> file_manager.parse_model_file sourced bounds+units FROM the cell (no bare-val
      fallback), so a stripped cell silently dropped params + raised "Could not detect time unit" in the
      9 scripts + FDT/REDUCTION/CROSSVAL builders. Fix: migrated cli._parse_cell to a DECOUPLED path
      (values from cell + bounds/order from Resources/Bounds/<model>_bounds.txt + units from
      Resources/Units/<model>_units.txt; merge iterates the bounds dict so cell params absent from bounds
      DROP -> bp rescale/forcing stay empty; parse_model_file kept as fallback; same 7-tuple return so no
      script/builder edits). Created hopf_bounds/units + bp_bounds(17 ND only)/units. Verified: parity on
      all 8 cells, SBI inject_ground_truth on nadrowski_cell_2 (13-dim, within-bounds), reparam_selftest
      full path PASS. NOTE: nadrowski_cell_1 f_max=7.55 is OUT of nadrowski_bounds.txt (0.01,5.0) --
      PRE-EXISTING, not a regression (don't use cell_1 as an SBI GT here, or widen f_max bound).
      Historical context of the refactor:
      SBI startup was refactored (2026-07-13) to DECOUPLE bounds from ground-truth values into
      THREE files -- Resources/Bounds/<model>_bounds.txt (parameter bounds; the only startup prompt),
      Resources/Units/<model>_units.txt (units; auto-resolved per model), and Resources/Cells/<name>.txt
      (a ground-truth POINT = values + inits, used only for "infer on a simulated dataset"). The parser
      file_manager.parse_values_file TOLERATES leftover bounds/units on a cell line (it takes the value
      and ignores the rest), so the current cell files still work -- but they are NOT clean: they still
      carry `= val in (lo,hi)` and `(units)` annotations that now belong in the Bounds/Units files.
      FIX (data-file hygiene, no code change -- the parser already handles values-only): strip every
      Resources/Cells/*.txt to pure `name = val` per ND / Dimensional / Forcing param, plus the
      `# Non-dimensional Initial Conditions` section. Drop the ` in (lo,hi)` bounds and the `(units)`
      annotations. Affected: nadrowski_cell.txt, nadrowski_cell_1/2/3.txt (+ hopf_cell.txt, bp_cell_*.txt
      if those models are used). Verify after: `python -m core` -> SBI -> NADROWSKI -> pick
      nadrowski_bounds.txt -> (load/train posterior) -> inference prompt "simulated" -> a cleaned
      nadrowski_cell_2.txt still injects the GT + passes the within-bounds safeguard (SimConfig.
      inject_ground_truth) and reproduces the corner/PPC/eye-test.

PROJECT & GOAL
  Simulation-based inference (amortized NPE via the `sbi` package, neural spline flow)
  to recover 13 parameters of the Nadrowski hair-bundle model from a single observable
  x(t): 10 nondimensional (ND) model params + 3 rescale params. Goal: a well-CALIBRATED
  and (where possible) precise posterior, assessed by SBC / TARP / PPC / eye-test.

  Parameter meanings (all defined from dimensional biophysical quantities):
    ND (10):     kappa=k_sp/k_gs, lambda=lambda_a/lambda_hb, phi=f_max, tau, tau_c, S,
                 dG, beta, N, temp(=T_a/T).        (order = NADROWSKI_LABELS in config.py)
    rescale (3): x_scale=D (gating swing), t_scale=lambda_hb/k_gs (relaxation time), f_scale.
                 [2026-07-01] The additive OFFSETS x_offset/t_offset/f_offset were REMOVED from the
                 inferred set (no basis in the model math; were pinned to +/-1e-6~=0). 16->13 dims.
                 Order now = [x_scale, t_scale, f_scale]; Hopf keeps only [x_scale, t_scale].
  NOTE: the Reduction / NWK->Hopf branch is being reworked and is IRRELEVANT to SBI.
  Do not invoke or reason about the reduction map for this work.

ENVIRONMENT / HOW TO RUN
  - conda env "biophys-env":  C:\Users\J\anaconda3\envs\biophys-env\python.exe
  - Full pipeline (interactive):
        python -m core  ->  SBI mode  ->  NADROWSKI  ->  Resources/Cells/nadrowski_cell_2.txt
        ->  choose/build prior  ->  TRAIN-NEW or load posterior  ->  validate  ->  (optional) infer.
  - Tools (non-interactive; env knobs documented in each file's docstring). Run as:
        & "C:\Users\J\anaconda3\envs\biophys-env\python.exe" scripts/<name>.py
        scripts/sbc_characterize.py        repeated SBC (K x n_cal) on a saved posterior -> KS-p
                                           distribution + (if present) t_offset rank-hist/marginals
                                           (POST/K/N_CAL). t_offset diag auto-skips when the cell has no
                                           t_offset; the general KS study still runs on all params.
        scripts/degeneracy_map.py          per-param feature-Jacobian |cos|/SVD/unique-handle at GT.
        scripts/feature_candidate_test.py  Laplace marginal-SD: does a candidate feature shrink a
                                           param's posterior? (per-param Fisher, prior-range-scaled).
        scripts/identifiability_offgt.py   Laplace SD at GT + prior samples (is info prior-wide?).
        scripts/retrain_convergence.py     non-interactive retrain + saves loss curve (DRY=1 = wiring).
        scripts/reparam_selftest.py        validate the reparam machinery (no retrain).
        scripts/reparam_wiring_smoke.py    tiny rotated train end-to-end (no real retrain).
        scripts/diagnose_fmax.py           older f_max feature-sensitivity + Part-B degeneracy probe.
        scripts/diagnose_fscale.py         WHY a rescale param mis-calibrates: rotation-mixing (deployed V)
                                           vs box-coordinate (linear vs log latent geometry). No retrain.
  - Syntax check:  py -3 -m py_compile <path>
  - GIT: make LOCAL changes only; the user handles all git / remote operations.

KEY FILES
  core/SBI/statistics.py   41-feature summary stats (Groups A-G); FEATURE_LABELS = order.
  core/SBI/pipeline.py     gen_obs; gen_stats; gen_training_data (Sobol (t_scale,T) batches);
                           train_nn (sbi NPE/SNPE; now also captures the per-epoch train/val loss).
  core/SBI/reparam.py      latent<->physical box bijection T (UnitToBoxTransform: per-param LINEAR or
                           LOG) + TransformedPosterior; the decorrelating rotation (OrthogonalTransform,
                           fisher_eigenbasis, build_rotated_bijection, RotatedLatentPrior); and
                           load_eval_bijection(cfg, POST, dir) = the self-describing eval-box rebuilder
                           (reads the {V, log_params} sidecar). build_*_bijection take a log_params arg.
  core/SBI/decorrelate.py  build_latent_fisher_rotation(cfg, T, latent_prior=, n_points=) -> V; now
                           AVERAGES the simulation Fisher over GT + prior draws (sims only).
  core/SBI/analysis.py     posterior_predictive_check; gen_cal_data (SBC calibration data).
  core/SBI/Priors/         stability-filtered GMM ND prior (construct_prior takes log_mask) + rescale/
                           forcing priors + ProductPrior.
  core/Helpers/file_manager.py  save/load_mix_dist persist the ND prior's per-param log_mask.
  core/orchestrator.py     run(): generate_observations -> build_prior -> build_posterior ->
                           validate (PPC/SBC/TARP/eye-test) -> infer_from_experiment (real data).
  core/config.py           constants + hyperparams: capacity (NSF_*), training (TRAINING_*), SBC_N_CAL,
                           reparam flags (REPARAM_ROTATE, REPARAM_FISHER_M/DZ, REPARAM_FISHER_POINTS,
                           REPARAM_LOG_PARAMS).

PIPELINE DESIGN (orienting notes)
  - LATENT reparameterization: the flow trains in an unbounded latent z; T: z->theta maps to the
    physical box (per-param scaled sigmoid). TransformedPosterior wraps the trained latent
    DirectPosterior so .sample/.log_prob speak physical coords. The ND prior is stored as
    TransformedDistribution(latent_GMM, T_nd); build_posterior extracts the latent GMM to train on.
    A trained posterior carries its exact training prior at posterior.prior.gen_dist (a ProductPrior)
    -> the SBC/reparam tools extract it; no separate prior file is needed.
  - TWO-trajectory stats: per sample, simulate a spontaneous (zero-drive) run -> Groups A-F and a
    forced run -> Group G (lock-in to the known drive). A1 (absolute mean) is the only non-demeaned
    feature; everything else is shape/scale-invariant-ish.
  - Conditioning layout = [ S(41) | log(T_obs) | forcing ]; log(T) rides the summary pathway, forcing
    is a separate EmbeddedNet pathway. Keep this order in sync across generate_observations /
    gen_training_data / validate / infer_from_experiment.
  - Scale-batched training: each batch shares a Sobol-sampled (t_scale, T); simulate at fine ND dt
    then downsample to dt_exp; pre-filter by N_ND_MAX.
  - GOTCHA: the latent prior is MIXED-DEVICE (cpu rescale bijection + cuda nd GMM). The pipeline only
    ever SAMPLES it, never .log_prob -- keep it that way (the reparam path also only samples it).

CURRENT POSTERIOR / CALIBRATION STATE
  posterior_3d.pt   = the "run-2" baseline, trained on nadrowski_cell_2.txt with REPARAM_ROTATE=False
                      (plain LINEAR pipeline; predates the reparam work). No .rot.pt sidecar.
  posterior_new.pt  = run-3, trained with REPARAM_ROTATE=True (GT-only Fisher rotation, LINEAR box).
                      Carries posterior_new.rot.pt (a bare tensor V = legacy sidecar format).
                      Loss converged (best val -0.282 @ epoch 84).
  posterior_6302026.pt = run-4 (Track A+), LOG box (k,lam,x_scale,t_scale,f_scale) + MULTI-POINT
                      averaged rotation, on a REBUILT prior. Sidecar = dict {V, log_params}.
                      RESULT below: multi-point HELPED (N/dG/tau_c recovered); LOG-space HURT the
                      degeneracy params (x_scale/t_scale/kappa/lambda) -> net worse. Not the keeper.
  posterior_07012026.pt = run-5 = THE KEEPER (2026-07-12). 13-DIM (offsets removed) LINEAR box +
                      MULTI-POINT averaged rotation. Sidecar {V:13x13, log_params:[]}. Converged (179 ep).
                      K=10x2000 result below: MEETS the calibrated-JOINT-posterior bar (conservatively).
                      Adversarial-panel verdict = borderline-MEETS: LOCKED IN, with 2 honest caveats
                      (x_scale ceiling residual + f_scale NEW in-scope tilt). Not a clean bill of health.

  Calibration: TRUST the K=10 x n_cal=2000 repeat study (scripts/sbc_characterize.py). A single
  n_cal=1000 SBC is UNDER-POWERED and hides the mild miscalibration (KS power grows with n_cal).
    Repeat-study KS-p median (frac of 10 runs with p<0.05), worst first:
      BASELINE posterior_3d (linear, no rotation):
        lambda 0.000(1.0)  tau_c 0.002(0.9)  x_scale 0.003(0.9)  t_offset 0.005(0.8)
        tau 0.007(0.8)  kappa 0.024(0.6)  phi 0.060(0.5)  beta 0.082(0.4)
        PASS: t_scale, f_offset, N, dG, S, temp, x_offset, f_scale.
      ROTATED  posterior_new (GT-only rotation, LINEAR box): a REDISTRIBUTION, not a net win --
        big wins: kappa 0.024->0.238, t_offset 0.005->0.366, phi 0.060->0.104;
        stuck:    lambda 0.000->0.000, tau 0.007->0.003;
        REGRESSED (were PASS): N ->0.005(1.0), dG ->0.014(0.8), S/temp/t_scale mild.
        Net KS-flagged count ~flat (6 -> 7). Pooled rank hists are ~FLAT for every param
        (mean rank 0.48-0.51, no bias; failing dims mildly UNDER-confident) -> the absolute
        miscalibration is MILD; KS at n_cal=2000 just flags small systematic residuals.
      LOG+AVG  posterior_6302026 (LOG box + multi-point avg rotation): NET WORSE on the targets --
        DEGENERACY params degraded:  x_scale 0.000(1.0) [catastrophic], t_scale 0.000(0.9),
          kappa 0.008(0.8) [lost the rot win], t_offset 0.015(0.7), lambda 0.001(0.7), tau 0.002,
          beta 0.003(0.8), phi 0.005(0.8);
        OFF-GT params RECOVERED (multi-point worked): N 0.312(0.2), dG 0.312(0.0), tau_c 0.585(0.0);
          f_scale 0.112, temp 0.146, S 0.272 ok-ish.
        SPLIT DIAGNOSIS: (3) multi-point averaging fixed the N/dG off-GT regression as intended;
        (2) LOG-space BACKFIRED on exactly the multiplicative-degeneracy params and erased the
        kappa/t_offset wins. f_scale (a log scale param NOT in a passive-trace degeneracy) stayed fine
        -> it's the degeneracy geometry, not log per se: the products aren't clean power laws, so log
        didn't linearize them. CONFOUND: log forced a prior rebuild (rebuild artifact not yet excluded).
      KEEPER posterior_07012026 (13-dim, LINEAR box + multi-point avg rotation), K=10x2000:
        KS-p median (frac<.05), worst first -- all MILD, all hists ~flat, NO overconfidence anywhere:
          x_scale 0.000(1.0), f_scale 0.004(0.8), phi 0.006(0.8), kappa 0.008(0.6), S 0.021(0.6),
          dG 0.057(0.5), N 0.077(0.4);  PASS-ish: lambda 0.170, temp 0.187, tau_c 0.259, tau 0.310,
          beta 0.312, t_scale 0.444.
        WINS vs 6302026: t_scale 0.000->0.444 (log disaster fixed), lambda 0.001->0.170, t_offset dim gone.
        FAILURE MODE (pooled 20k ranks): EVERY hist ~flat within ~3pp; max |mean-0.5|=0.031 (x_scale);
          every tailTot<=0.20 (slightly WIDE = safe, NEVER overconfident). TARP(joint) KS p=1.000,
          ATC +0.322 (conservative lean). PPC mean|z|=0.398, 0/46 outside.
        VERDICT (adversarial for/against panel, borderline-MEETS the calibrated-JOINT bar, conservatively):
          CAVEAT 1 (OUT of scope): x_scale ~3% one-sided OVER-estimate is a LOCATION bias (biases the
            point est / product D*A_nd HIGH), not width -- but x_scale is the chronic info-ceiling param
            (never calibrated in ANY config); safe for INTERVAL coverage. Quote intervals, not point values.
          CAVEAT 2 (IN SCOPE, NEW, DIAGNOSED 2026-07-12): f_scale ~1.5% one-sided UNDER-estimate. f_scale
            is NOT a ceiling param and PASSED in all 3 prior runs. DIAGNOSED (scripts/diagnose_fscale.py):
            NOT the rotation (f_scale self-conc 1.00, degen-overlap 0.00 in the deployed V -- rides its own
            axis). CAUSE = the LINEAR box on a 3-decade positive scale param (bounds 1-1000, GT=10 at
            box-fraction 0.009 / latent z=-4.7; 57% of the log-uniform prior mass has |z|>3 = flat sigmoid
            tail the fixed-knot flow mis-resolves). FIX (retrain to confirm): REPARAM_LOG_PARAMS=['f_scale']
            -- log f_scale ONLY (keep degeneracy params LINEAR); f_scale is a RESCALE param => NO ND prior
            rebuild (guard passes). Too mild to block the keeper; do NOT file under the degeneracy.
          Do NOT oversell TARP=1.000 (at ceiling partly from the conservative/wide lean; TARP is less
            sensitive than marginal SBC). Honest claim = "no detectable overconfidence; coverage
            indistinguishable from ideal", NOT "perfectly calibrated joint".
    PPC clean (mean|z|=0.35); TARP excellent (ATC -0.064). Both are less sensitive than marginal
    SBC, so they coexist with the mild marginal biases above (no contradiction).
    sbc_characterize.py is now ROTATION/LOG aware: it reconstructs each posterior's EXACT training
    box (log + rotation) from its .rot.pt sidecar via reparam.load_eval_bijection (cfg, POST, dir).
    Old posteriors with no sidecar (or a bare-tensor V) load as linear -- backward compatible.

KEY FINDING (the diagnosis driving the current fix)
  The SBC failures are NOT an information / degeneracy deficit -- they are FLOW CALIBRATION.
  Evidence chain:
    - CONVERGED, not under-fit: a convergence retrain plateaued (val loss flat through the full
      early-stop window; small train-val gap) -> more training/capacity won't help.
      (scripts/retrain_convergence.py; the script's auto "under-fit" line is a noise-spike artifact.)
    - DEGENERACY is real but not fatal: dominant aliases kappa~x_scale |cos|=0.94 (amplitude: x(t)
      sees the product D*A_nd) and lambda~t_scale |cos|=0.96 (timescale), in a broadly sloppy
      manifold. (scripts/degeneracy_map.py)
    - BUT identifiability is SUFFICIENT prior-wide: a Laplace marginal-SD analysis (Fisher F=J^TJ from
      sims) shows every param pinned to ~0.3-7% of its prior range, holding across GT + prior samples
      (kappa/lambda/x_scale/t_scale identified in 100% of K=6 points), EXCEPT t_offset & f_scale which
      are genuinely uninformative (SD~1). (scripts/feature_candidate_test.py, identifiability_offgt.py)
  => The params are well-identified, but their joint posterior is a thin, 0.95-correlated ridge that
     the flow mis-calibrates (tight posteriors are hypersensitive to small flow biases in SBC).
     t_offset's mild failure = the flow not perfectly reverting to the prior on an uninformative dim;
     f_scale passes because the flow reverts to prior well there.

THE FIX (Track A) -- IMPLEMENTED + VALIDATED + RETRAINED + EVALUATED (result: PARTIAL, see below)
  Decorrelating reparameterization: rotate the flow's latent coordinate into the simulation-based
  Fisher eigenbasis V so the correlated posterior becomes axis-aligned and the flow can calibrate it.
    - V = decorrelate.build_latent_fisher_rotation(cfg): from the latent feature-Fisher (sims only,
      NO trained posterior) -> generalizes to any model. V=I (REPARAM_ROTATE=False) = plain pipeline.
    - Wired into build_posterior (computes V, trains rotated, saves <name>.rot.pt beside the .pt),
      and the load path + validate reuse the saved V (read from posterior.T).
    - Keeps ALL params individually -- orthogonal/invertible, log-det 0; no information loss, no
      model/stats change. Validated by reparam_selftest.py + reparam_wiring_smoke.py (decorrelation
      Fisher off-diag 0.77 -> ~0; bijection round-trip, in-box, log-det preserved; rotated train +
      save/load/sample all pass) -- no retrain needed to confirm correctness.

  RESULT (posterior_new, K=10 x 2000): PARTIAL = a redistribution, not the predicted clean win
  (see CURRENT STATE table). The rotation is genuine (kappa~x_scale loadings overlap 0.73,
  lambda~t_scale 0.86; V is NOT a permutation), and it fixed kappa & t_offset -- but it broke N & dG
  (were PASS) and left lambda/tau stuck. DIAGNOSIS: V was computed at a SINGLE point (GT) and the box
  was LINEAR, but the dominant degeneracies are MULTIPLICATIVE (kappa*x_scale, lambda*t_scale ~ const
  = hyperbolae). A single linear rotation straightens the GT TANGENT but re-correlates off-GT (breaks
  N/dG) and cannot globally straighten the most-curved ridge (lambda stuck). Textbook ceiling of a
  single-point linear decorrelation.

THE FIX, PART 2 (Track A+) -- IMPLEMENTED + VALIDATED + RETRAINED (posterior_6302026);
                             RESULT = multi-point HELPED, LOG-space HURT (see CURRENT STATE)
  Two changes attack the curvature + single-point limits found above:
   (2) LOG-SPACE BOX: params in config.REPARAM_LOG_PARAMS (default k, lam, x_scale, t_scale, f_scale)
       use a GEOMETRIC box  theta = lo*(hi/lo)^sigmoid(z)  instead of linear. In log coords the
       products become SUMS, so the linear rotation can decorrelate them prior-wide. Only lo>0 params
       eligible. reparam.UnitToBoxTransform implements it; threaded through the ND prior (gen_prior ->
       construct_prior), the rescale prior (auto via T_rescale.inv), and serialization (file_manager
       persists the per-param log_mask). The log_mask + V are saved in the <name>.rot.pt sidecar
       (now a dict {V, log_params}); load_eval_bijection rebuilds the EXACT box, so eval never depends
       on the current config. Backward compatible: no sidecar => linear; bare-tensor sidecar => V only.
   (3) MULTI-POINT FISHER: decorrelate.build_latent_fisher_rotation now AVERAGES F over GT + prior
       draws (config.REPARAM_FISHER_POINTS=8, pass latent_prior=). Averaging makes the single linear
       rotation valid across the prior, not just at GT (fixes the N/dG off-GT regression).
  Validated (no retrain needed for correctness): reparam_selftest.py PASSES with the log box (V
  orthogonal/diagonalizes F off/diag 0.57->3e-8; rotated-log round-trip 4e-6; in-box; LOG-DET ==
  box log-det 8e-6); log-box geometric mapping + round-trip + masks + backward-compat load all
  checked; multi-point loop averages over N points and returns an orthogonal V.

  ARTIFACTS RULED OUT (2026-07-01, on-disk read-only) -- the "log hurt" result is REAL, not an
  artifact, but its SEVERITY is overstated by the strict KS-at-2000 ruler:
    1. Convergence [OK, not underfit]: posterior_6302026.loss.npz trained 186 epochs, best val
       -1.135 @ ep165, clean early-stop (patience 20), val NLL <= train. (The -1.13 vs posterior_new's
       -0.28 is NOT a quality gap -- the log-box Jacobian shifts absolute NLL; cross-coord losses
       are not comparable.)
    2. Eval box [OK]: posterior_6302026.rot.pt = dict {V(16x16 orthogonal, |V^TV-I|=1.2e-7, det 1.0),
       log_params=[k,lam,x_scale,t_scale,f_scale]}, 5/16 log dims; load_eval_bijection reconstructs
       the exact box (SBC was evaluated in the right coordinates).
    3. Failure MODE [mild bias, NOT a catastrophe]: pooled rank hists (ranks.npz) are ~FLAT with a
       small MONOTONE tilt -- x_scale mean 0.538 (rising ramp), t_scale 0.529, kappa 0.518, lambda
       0.487, t_offset 0.484; tau_c (passing) 0.500 flat. NOT U (overconf), NOT cap (underconf), NOT
       multimodal (phase-wrap). "x_scale 0.000(1.0)" is the n=2000 KS flagging a ~4% mean shift --
       same mild character as posterior_new, not a new pathology.
    4. Prior rebuild [sound] + V geometry [the mechanism]: rebuilt log-GMM 78 comps vs linear 77
       (comparable, not degenerate). LINEAR GT-only V (posterior_new) captures BOTH aliases cleanly
       (a kappa~x_scale mode + a lambda~t_scale mode); LOG multi-point V (6302026) captures kappa~x_scale
       but SCATTERS lambda across tau_c/x_scale with NO clean lambda~t_scale mode (rotation overlap
       ~0.775) => in log-coords the products did NOT linearize; the rotation OVER-MIXED. The one confound
       disk can't separate (log + multi-point changed together) is exactly what the clean control isolates.

  NEXT EXPERIMENT (RUNNING 2026-07-01): 13-DIM LINEAR box + multi-point averaged rotation. The OFFSET
  removal (16->13) supersedes/subsumes the earlier "control" -- same knobs, cleaner space:
    config: REPARAM_ROTATE=True, REPARAM_FISHER_POINTS=8, REPARAM_LOG_PARAMS=[]  (empty => LINEAR box)
    python -m core -> SBI -> NADROWSKI -> nadrowski_cell_2 -> REUSE the existing ND prior
      prior_forcing_no_forcing.pt (10-dim ND GMM, rescale-INDEPENDENT so unaffected by offset removal;
      guard passes on the linear mask -- do NOT rebuild, do NOT pick rotated_prior.pt) -> TRAIN-NEW ->
      save e.g. posterior_13d_linrot (sidecar {V:13x13, log_params:[]}). Re-check with sbc_characterize.py
      (13 params, no t_offset row; header "rotation=on, log-box dims=0/16").
    HYPOTHESIS: keeps the kappa win (rotation) AND N/dG fixed (averaging) WITHOUT the log regression, in a
      cleaner 13-dim space with the 3 inert offset dims (incl. the mildly-failing t_offset) gone -> likely
      the keeper; also confirms log-space is harmful (retire it).
  Prior-rebuild rule (IMPORTANT nuance): only ND log params (k/lam/... in REPARAM_LOG_PARAMS) force an ND
  prior rebuild (build_posterior raises on nd_log_mask mismatch). RESCALE-only log params (e.g.
  ['f_scale']) do NOT -- nd_log_mask stays all-False, so the existing linear ND prior passes the guard.
  This is why the staged f_scale fix (REPARAM_LOG_PARAMS=['f_scale']) reuses prior_forcing_no_forcing.pt
  with no rebuild. retrain_convergence.py refuses a rotated base.
  Old 16-dim posteriors (posterior_3d/new/6302026) are now OBSOLETE -- they fail loudly (shape mismatch /
  16x16 V vs 13-dim box) against the 13-dim cell; keep for record only.

STEP STATUS (original handoff plan)
  1. [DONE] Eye test overlays the posterior MEAN + MEDIAN parameter trajectories (not the MAP).
  2. [DONE] t_offset = real MILD high-bias, not noise (it's flow-reversion on an uninformative dim).
  3. [DONE] Convergence check -> CONVERGED, not under-fit.
  4. [DONE] Degeneracy map -> kappa~x_scale (0.94), lambda~t_scale (0.96) + sloppy core.
  5. [DONE] Flow-calibration fix = decorrelating reparam, retrained (posterior_new) + evaluated.
     Result = PARTIAL/redistribution (see THE FIX RESULT). Diagnosis: GT-only + linear-box ceiling.
  6. [DONE] Track A+ = log-space box (2) + multi-point Fisher (3), retrained (posterior_6302026) +
     evaluated. Result: multi-point HELPED (N/dG/tau_c recovered); LOG-space HURT the degeneracy
     params (x_scale/t_scale/kappa/lambda) -> net worse. Not the keeper.
  7. [DONE] Ruled out artifacts on posterior_6302026 (convergence / eval-box / failure-mode / prior+V):
     "log hurt" is REAL but MILD (flat rank hists); log OVER-MIXED instead of linearizing the products.
     See ARTIFACTS RULED OUT.
  8. [DONE, code] Removed the additive OFFSET rescale params (x_offset/t_offset/f_offset) from the
     inferred set for Nadrowski + Hopf (16->13); offsets now optional/guarded (default 0). See THIS-SESSION.
  9. [DONE] Retrained 13-dim posterior_07012026 (LINEAR box + multi-point averaged rotation) + K=10x2000
     re-check. See KEEPER block. t_scale/lambda recovered; x_scale (ceiling) + f_scale (NEW) mild-fail.
 10. [DONE] Adversarial for/against panel verdict = borderline-MEETS the calibrated-JOINT bar -> LOCKED IN
     as THE KEEPER. Open item: f_scale new in-scope tilt (likely rotation over-mixing) -- optional diagnostic.

WHAT'S NEXT
  - [DONE] KEEPER LOCKED: posterior_07012026 (13-dim LINEAR box + multi-point rotation) MEETS the
    calibrated-JOINT bar (conservatively; no overconfidence on any of the 13 dims). See KEEPER block + caveats.
  - [DIAGNOSED 2026-07-12, fix pending retrain] f_scale NEW in-scope mild tilt (~1.5% under-est):
    scripts/diagnose_fscale.py showed it is NOT the rotation (f_scale unmixed in the deployed V, self-conc
    1.00 / degen-overlap 0.00) but the LINEAR box on a 3-decade positive scale param (GT=10 at box-fraction
    0.009 / latent z=-4.7; 57% of prior mass in the flat sigmoid tail). It is collateral of the all-linear
    REPARAM_LOG_PARAMS=[] (the degeneracy params NEED linear; f_scale needs log). FIX: set
    REPARAM_LOG_PARAMS=['f_scale'] (log f_scale ONLY), reuse prior_forcing_no_forcing.pt (f_scale is
    rescale -> NO ND prior rebuild, guard passes), TRAIN-NEW, re-check (header 'log-box dims=1/13').
    Not a correctness blocker; do before freezing the amortized reference.
  - [DONE] Ruled out artifacts on posterior_6302026: the "log hurt" conclusion is REAL (not a
    rebuild/underfit artifact) but MILD (see ARTIFACTS RULED OUT).
  - Consider whether KS-at-2000 is even the right bar: on the earlier runs pooled rank hists were ~flat
    and PPC/TARP good, so a posterior may be practically calibrated even when KS flags it. Strict ruler.
  - Confound: posterior_3d predates pipeline changes; part of any delta could be retrain variance.
    Cleanest attribution = same-pipeline REPARAM_ROTATE=False control (expensive); skip unless needed.
  - [DEFERRED -- user: "later"] MULTI-FREQUENCY chi(omega) DATA = the only lever for the INFORMATION
    ceiling (orthogonal to calibration, and the source of KEEPER CAVEAT 1's x_scale ~3% bias): pinning
    kappa/lambda/x_scale/t_scale *individually* much tighter -- and removing x_scale's residual location
    bias -- needs new INFORMATION. A single passive trace only sees the products D*A_nd (amplitude) and
    (lambda_hb/k_gs)*tau_nd (timescale); no reparam/box/rotation can manufacture the missing separation
    (every config tried has left x_scale mis-centered). The strong separator is the FORCED susceptibility
    chi(omega) SHAPE over multiple drive frequencies (reuse the FDT machinery), which needs a
    multi-frequency drive in BOTH training and experiment. This is the natural NEXT PROJECT once the
    f_scale polish lands; it is out of scope for the current calibrated-joint-posterior bar.
  - Track B (add chi(omega)/waveform-shape features) is LOW-VALUE for kappa/lambda (info already
    present, ~5-7% SD gain). The first candidate batch (feature_candidate_test.py) DID tighten
    N/temp/tau/tau_c/beta by 19-36%, so revisit only to pin THOSE tighter. (rise_fall candidate dead.)

THIS-SESSION CHANGES (2026-06-22..07-01; all local; code only unless noted)
  - [2026-07-01] REMOVED the OFFSET rescale params (x_offset/t_offset/f_offset) from the inferred set
    for Nadrowski + Hopf (no basis in the model math; were pinned to +/-1e-6~=0). Inferred 16->13 (10 ND
    + x_scale/t_scale/f_scale); Hopf -> 2 rescale. Deleted the offset lines from all Nadrowski cells
    (cell, cell_1/2/3) + hopf_cell; made offset access OPTIONAL/GUARDED (default 0) in pipeline.py,
    orchestrator.py, decorrelate.py + 6 scripts (mirrors the `if "f_scale" in rescale_idx` pattern; BP
    cells untouched -- their rescale lines lack `in (...)` bounds so never parse into rescale_params).
    LOAD-BEARING fix: pipeline's `if "f_scale" in rescale_idx` branch still reads f_offset -> guard it.
    Everything else auto-shrinks from cfg.rescale_params (no hardcoded 16/6; all access by name).
    Verified: py_compile (9 files) + smoke test (13-dim NAD, 2-rescale HOPF, bijection round-trips,
    rescale prior 3-dim, force-builder OK). sbc_characterize.py keeps its general KS study and auto-skips
    the now-moot t_offset deep-dive. Old 16-dim posteriors are obsolete (loud shape-mismatch fail).
  - [2026-07-01, analysis] RULED OUT artifacts on posterior_6302026 (convergence / eval-box /
    failure-mode / prior GMM + V geometry): the "log hurt" result is REAL but MILD; log OVER-MIXED
    rather than linearizing the products. See ARTIFACTS RULED OUT.
  - [2026-07-01, RUN not code] Retrained run-4 posterior_6302026 (Track A+) + K=10x2000 re-check:
    multi-point averaging HELPED (N/dG/tau_c recovered), LOG-space HURT the degeneracy params
    (net worse). Conclusion: next = LINEAR box + averaged rotation (see WHAT'S NEXT / FIX PART 2).
  - sbc_characterize.py / identifiability_offgt.py were applying the PLAIN box to a ROTATED posterior
    (silent wrong SBC). FIXED: both reconstruct the exact box via reparam.load_eval_bijection(cfg,
    POST, dir), now the single source of truth (orchestrator load path uses it too). retrain_
    convergence.py guards against a rotated base (no rotated-retrain path).
  - (2) LOG-SPACE BOX: reparam.UnitToBoxTransform + log_mask threaded through build_*_bijection,
    gen_prior -> Prior.construct_prior, and file_manager save/load_mix_dist. config.REPARAM_LOG_PARAMS
    = [k, lam, x_scale, t_scale, f_scale]. The <name>.rot.pt sidecar is now a dict {V, log_params};
    load_eval_bijection rebuilds the exact box (backward compatible with no-sidecar / bare-tensor V).
  - (3) MULTI-POINT FISHER: decorrelate.build_latent_fisher_rotation averages F over GT + prior draws
    (config.REPARAM_FISHER_POINTS=8); build_posterior passes latent_prior= so it's automatic.
  - build_posterior GUARD: raises if a loaded ND prior's log_mask != config (forces a prior rebuild).
  - [PRIOR SESSION] eye-test; loss-curve capture; SBC n_cal=2000; prior global-sweep off-by-one fix;
    Reparam (Track A) machinery + REPARAM_ROTATE flag (now default True).

CONVENTIONS / GOTCHAS
  - Cell file: nadrowski_cell_2.txt. Its param bounds define the bijection and MUST match training.
  - RESCALE offsets are OPTIONAL/GUARDED: x_offset/t_offset/f_offset default to 0 when absent from the
    cell (mirrors `if "f_scale" in rescale_idx`). Nadrowski/Hopf cells dropped them (now 13-dim); BP cells
    keep them and still work. A posterior's dim = 10 ND + len(cfg.rescale_params); a saved posterior only
    loads against a cell with the SAME rescale set (else a loud shape mismatch, never silent).
  - A saved posterior is described by (bounds from cell file) + (log_mask) + (rotation V). The last two
    live in the <name>.rot.pt sidecar; ALWAYS reconstruct the eval box via load_eval_bijection(cfg,
    POST, dir), never a bare build_inferred_bijection(cfg) -- config may have drifted since training.
  - LOG-SPACE box: changing config.REPARAM_LOG_PARAMS REQUIRES rebuilding the ND prior (the latent GMM
    is fit in the box coordinate); build_posterior raises on a mismatch. Only lo>0 params are eligible.
  - Trust SBC KS p-values, not c2st_ranks (~0.58 is the c2st finite-sample floor, ~uniform across params).
  - SBC power: use n_cal >= 2000 (validate uses config.SBC_N_CAL=2000); a single n_cal=1000 under-reports.
    But pooled rank HISTOGRAMS read the severity KS can't: ~flat = mild even when KS p is low at n=2000.
  - Signal generally improves with longer T_obs recordings (esp. for the weakly-imprinted params).
  - Mixed-device latent prior: SAMPLE only, never .log_prob (see PIPELINE DESIGN gotcha).
  - Reduction / NWK->Hopf branch is irrelevant to SBI (being reworked) -- ignore it here.
================================================================================
